extends layout

block content
    .users-header
        h1 Список користувачів
        
        // Кнопка "Додати користувача"
        if isAuthenticated
            .user-actions.mb-3
                a.btn.btn-success(href="/register") ➕ Додати користувача
                
    .users-grid
        each user in users
            .user-card
                img(src=user.avatar, alt=user.name)
                h2= user.name
                p= user.position
                p.email= user.email
                
                .card-actions.mt-2
                    // Кнопка "Детальніше"
                    a.btn.btn-info(href=`/users/${user._id}`) Детальніше
                        
                    // Кнопки керування (для авторизованих)
                    if isAuthenticated
                        // Кнопка "Редагувати"
                        a.btn.btn-sm.btn-secondary(href=`/users/edit/${user._id}`) ✍️ Редагувати
                        
                        // Кнопка "Видалити" — button повинен бути всередині form
                        form.d-inline(action=`/users/delete/${user._id}`, method="POST", onsubmit=`return confirm('Ви впевнені, що хочете видалити користувача ${user.name}?');`)
                            button.btn.btn-sm.btn-danger(type="submit") 🗑️ Видалити
