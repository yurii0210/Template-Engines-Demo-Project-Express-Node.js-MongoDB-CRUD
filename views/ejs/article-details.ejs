<!DOCTYPE html>
<html lang="uk">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title><%= title %></title>
    <link rel="icon" href="/favicon.ico">
    <link rel="stylesheet" href="/css/style.css">
    <link rel="stylesheet" href="/css/articles.css">
</head>
<body>
    <header>
        <nav>
            <a href="/" class="<%= currentPath === '/' ? 'active' : '' %>">Головна</a>
            <a href="/users" class="<%= currentPath === '/users' ? 'active' : '' %>">Користувачі</a>
            <a href="/articles" class="<%= currentPath === '/articles' ? 'active' : '' %>">Статті</a>

            <% if (isAuthenticated) { %>
                <a href="/profile" class="<%= currentPath === '/profile' ? 'active' : '' %>">Профіль</a>
                <a href="/logout">Вийти</a>
            <% } else { %>
                <a href="/login" class="<%= currentPath === '/login' ? 'active' : '' %>">Вхід</a>
                <a href="/register" class="<%= currentPath === '/register' ? 'active' : '' %>">Реєстрація</a>
            <% } %>
        </nav>
    </header>

    <main class="container">
        <a href="/articles" class="btn btn-sm btn-info back-btn">← Назад до списку</a>
        
        <article class="article-full">
            <img src="<%= article.image %>" alt="<%= article.title %>">
            <div class="article-header">
                <h1><%= article.title %></h1>
                <div class="meta">
                    <span class="author">Автор: <%= article.author %></span>
                    <span class="date"><%= article.date %></span>
                    <span class="category"><%= article.category %></span>
                </div>
                
                <% if (isAuthenticated) { %>
                    <div class="article-detail-actions mt-3 mb-4">
                        <a href="/articles/edit/<%= article._id %>" class="btn btn-primary">✍️ Редагувати статтю</a>
                        
                        <form action="/articles/delete/<%= article._id %>" method="POST" style="display: inline-block; margin-left: 10px;" onsubmit="return confirm('Ви дійсно хочете видалити статтю «<%= article.title %>»?');">
                            <button type="submit" class="btn btn-danger">🗑️ Видалити статтю</button>
                        </form>
                    </div>
                <% } %>
                
            </div>
            
            <div class="article-content">
                <p><%= article.content %></p>
                <p>Це повний текст статті. Тут може бути багато цікавого контенту...</p>
            </div>
            
            <div class="tags">
                <% article.tags.forEach(function(tag) { %>
                    <span class="tag"><%= tag %></span>
                <% }); %>
            </div>
        </article>
    </main>

    <footer>
        <div class="footer-content">
            <p>© 2025 Template Engines Server</p>
        <div class="footer-links">
            <a href="https://github.com/yurii0210/PUG-and-EJS-from-Express.git" target="_blank">GitHub</a>
    </div>
    </div>
    </footer>
</body>
</html>